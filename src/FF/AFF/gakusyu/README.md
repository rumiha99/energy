# FF/AFF/gakusyu

現在の標準入口は次のファイルです。

```text
cbam_full_from_images.py
```

旧 Notebook は比較・復元用に移動済みです。

```text
archive/notebooks/FF_AFF_gakusyu/
```

新しい学習処理は、可能な限り Notebook ではなく Python スクリプトとして追加します。実験で変える値は `configs/` に置き、結果は `runs/` に出します。

